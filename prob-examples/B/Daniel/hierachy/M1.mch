MACHINE M1(P,p)
CONSTRAINTS
  p:NAT
SETS
 ENUM={aa,bb};
 DEFERRED
CONSTANTS iv
PROPERTIES
 iv:ENUM
INCLUDES M2(P,p+3)
PROMOTES Set2
VARIABLES xx
INVARIANT
 xx:ENUM
INITIALISATION xx:=iv
OPERATIONS
  Set(yy) = PRE yy:ENUM THEN xx:= yy END;
  q <-- query(yy) = PRE yy:ENUM THEN q <-- query2 END
END

