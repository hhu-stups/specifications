MACHINE CounterArticle64_ok
DEFINITIONS SET_PREF_MAX_OPERATIONS == 0; SET_PREF_MAX_INITIALISATIONS == 0
CONSTANTS m
PROPERTIES m : {127,255}
VARIABLES c
INVARIANT c>=-2 & c /= -1
INITIALISATION c:=0
OPERATIONS
  incby(i) = PRE i:1..64 THEN c := c+i END
END
