MACHINE Apply
DEFINITIONS
 F==ff;
 "CallOrApply.def"
CONSTANTS ff,rr
PROPERTIES
 ff = %x2.(x2:INTEGER|x2+1) &
 rr = CallOrApply(1)
VARIABLES yy
INVARIANT yy:INTEGER
INITIALISATION yy := 0
OPERATIONS
  OpF(x3) = BEGIN yy := ff(x3) END 
END