MACHINE PrettyPrintBug
DEFINITIONS SET_PREF_SHOW_EVENTB_ANY_VALUES == TRUE
CONSTANTS Sum
PROPERTIES
Sum = {(1,2)}
VARIABLES x
INVARIANT
x : 1..10
INITIALISATION
x:= 1
OPERATIONS
Next = ANY p WHERE p : 1..10 & p = Sum(1) THEN  x := p END
END