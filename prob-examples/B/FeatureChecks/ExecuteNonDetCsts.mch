MACHINE ExecuteNonDetCsts
SETS
 ID={aa,bb}
CONSTANTS iv
PROPERTIES
 iv:ID
DEFINITIONS GOAL == (xx >= 100)
VARIABLES xx
INVARIANT
 xx:NATURAL
INITIALISATION xx:=0
OPERATIONS
  Run = PRE xx<100 THEN xx := xx+1 END
END

