REFINEMENT MachineWithParametersErr1(a) // parameter B missing
REFINES MachineWithParameters
//CONSTRAINTS a:NAT  // paser now complains about this
VARIABLES x
INITIALISATION x:=0
OPERATIONS
  step = IF x=a THEN x:=0 ELSE x:=x+1 END
END
